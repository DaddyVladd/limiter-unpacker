# API Tweaks NetLimiter Patcher

This is a one-click NetLimiter patcher made by **API Tweaks**. It embeds custom DLLs, supports rebranding, and auto-installs everything with no manual edits, no dnSpy, and no headaches.
